# 示例环境配置（复制为项目根目录的 `.env` 后使用）
# 注意：不要把真实密钥提交到版本库！

# —— OpenAI 配置 ——
# 必填：OpenAI API Key（服务端使用，前端不会暴露）
OPENAI_API_KEY=sk-your_openai_api_key_here

# 可选：自定义 OpenAI 兼容的基础地址（走官方可留空或保留默认）
# 例如：代理/网关/第三方兼容 OpenAI Responses API 的地址
# OPENAI_BASE_URL=https://api.openai.com/v1

# —— Anicca 默认模型 ——
# 当请求未显式传入 model 时，使用该默认模型名
ANICCA_DEFAULT_MODEL=gpt-4o-mini

# —— 其他说明 ——
# 1) 将本文件复制为项目根目录的 `.env` 并替换为你的真实值
# 2) Next.js 会在服务端通过 process.env 读取上述变量
# 3) 如果你使用自定义 OPENAI_BASE_URL，请确保与 OpenAI SDK/Responses API 兼容
